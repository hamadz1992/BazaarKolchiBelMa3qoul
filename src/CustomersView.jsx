import React,{useMemo,useState} from "react";
import {Search,Plus,UserRound,Phone,MapPin,Edit3,Trash2,X} from "lucide-react";
import "./customers.css";
const KEY="bazaar-kolchi-customers";
const initial=[{id:1,name:"زبون نقدي",phone:"",address:"",note:""}];
function load(){try{const x=JSON.parse(localStorage.getItem(KEY));return Array.isArray(x)?x:initial}catch{return initial}}
function save(x){localStorage.setItem(KEY,JSON.stringify(x))}
export default function CustomersView(){const[customers,setCustomers]=useState(load);const[q,setQ]=useState("");const[modal,setModal]=useState(null);const[form,setForm]=useState({name:"",phone:"",address:"",note:""});const filtered=useMemo(()=>customers.filter(c=>`${c.name} ${c.phone} ${c.address}`.toLowerCase().includes(q.toLowerCase())),[customers,q]);const open=c=>{setForm(c?{name:c.name,phone:c.phone,address:c.address,note:c.note}:{name:"",phone:"",address:"",note:""});setModal(c||"new")};const submit=e=>{e.preventDefault();if(!form.name.trim())return;let next;if(modal==="new")next=[...customers,{...form,id:Date.now()}];else next=customers.map(c=>c.id===modal.id?{...c,...form}:c);setCustomers(next);save(next);setModal(null)};const remove=id=>{if(!window.confirm("هل تريد حذف هذا العميل؟"))return;const next=customers.filter(c=>c.id!==id);setCustomers(next);save(next)};return <div className="customersView" dir="rtl"><div className="customersHeader"><div><h1><UserRound size={28}/> العملاء</h1><p>إدارة بيانات العملاء والبحث عنهم بسهولة</p></div><button className="customerPrimary" onClick={()=>open(null)}><Plus size={18}/> إضافة عميل</button></div><section className="customerPanel"><div className="customerToolbar"><div className="customerSearch"><Search size={18}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحث باسم العميل أو رقم الهاتف..."/></div><span>{filtered.length} عميل</span></div><div className="customerTableWrap"><table className="customerTable"><thead><tr><th>العميل</th><th>رقم الهاتف</th><th>العنوان</th><th>ملاحظات</th><th>إجراء</th></tr></thead><tbody>{filtered.map(c=><tr key={c.id}><td><div className="customerName"><span><UserRound size={17}/></span><strong>{c.name}</strong></div></td><td>{c.phone?<><Phone size={14}/> {c.phone}</>:"—"}</td><td>{c.address?<><MapPin size={14}/> {c.address}</>:"—"}</td><td>{c.note||"—"}</td><td><div className="customerActions"><button onClick={()=>open(c)} title="تعديل"><Edit3 size={16}/></button><button className="delete" onClick={()=>remove(c.id)} title="حذف"><Trash2 size={16}/></button></div></td></tr>)}</tbody></table></div>{!filtered.length&&<div className="customerEmpty">لا توجد نتائج مطابقة.</div>}</section>{modal&&<div className="customerBackdrop" onMouseDown={()=>setModal(null)}><form className="customerModal" onSubmit={submit} onMouseDown={e=>e.stopPropagation()}><div className="customerModalHead"><h2>{modal==="new"?"إضافة عميل":"تعديل العميل"}</h2><button type="button" onClick={()=>setModal(null)}><X size={20}/></button></div><label>اسم العميل<input autoFocus value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label><label>رقم الهاتف<input type="tel" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>العنوان<input value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></label><label>ملاحظات<input value={form.note} onChange={e=>setForm({...form,note:e.target.value})}/></label><div className="customerModalActions"><button type="button" onClick={()=>setModal(null)}>إلغاء</button><button className="customerPrimary" type="submit">حفظ</button></div></form></div>}</div>}
