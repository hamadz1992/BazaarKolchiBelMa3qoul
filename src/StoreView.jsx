import React,{useState} from 'react';
import {Store,Save,MapPin,Phone,Clock} from 'lucide-react';
import './store.css';
const KEY='bazaar_store_settings';
const defaults={name:'كل شيء بالمعقول',phone:'',address:'',hours:'09:00 - 21:00',status:true};
export default function StoreView(){const [form,setForm]=useState(()=>{try{return {...defaults,...JSON.parse(localStorage.getItem(KEY)||'{}')}}catch{return defaults}});const [msg,setMsg]=useState('');const set=(k,v)=>setForm({...form,[k]:v});const save=()=>{localStorage.setItem(KEY,JSON.stringify(form));setMsg('تم حفظ بيانات المتجر بنجاح')};return <div className="storeView"><div className="storeHeader"><div><h1><Store/> المتجر</h1><p>إدارة معلومات المتجر وحالته وأوقات العمل.</p></div><button onClick={save}><Save size={18}/> حفظ</button></div><section className="storeCard"><div className="storeIdentity"><div className="storeLogo"><Store/></div><div><h2>{form.name||'اسم المتجر'}</h2><span className={form.status?'openStatus':'closedStatus'}>{form.status?'المتجر مفتوح':'المتجر مغلق'}</span></div></div><div className="storeFields"><label><Store size={16}/> اسم المتجر<input value={form.name} onChange={e=>set('name',e.target.value)}/></label><label><Phone size={16}/> رقم الهاتف<input value={form.phone} onChange={e=>set('phone',e.target.value)} placeholder="اختياري"/></label><label><MapPin size={16}/> العنوان<input value={form.address} onChange={e=>set('address',e.target.value)} placeholder="اختياري"/></label><label><Clock size={16}/> أوقات العمل<input value={form.hours} onChange={e=>set('hours',e.target.value)}/></label></div><div className="storeStatus"><div><strong>حالة المتجر</strong><small>يمكن تغييرها عند إغلاق المتجر مؤقتًا.</small></div><button type="button" className={form.status?'switch on':'switch'} onClick={()=>set('status',!form.status)}><i/></button></div></section>{msg&&<div className="storeMessage">{msg}</div>}</div>}
