import React,{useState} from 'react';
import {Download,Upload,Database,Clock,ShieldCheck,RefreshCw} from 'lucide-react';
import {loadProducts} from './products-data.js';
import './backup.css';
const keys=['bazaar_products','bazaar_categories','bazaar_units','bazaar_inventory_movements','bazaar_customers','bazaar_sales'];
export default function BackupView(){const[last,setLast]=useState(()=>localStorage.getItem('bazaar_last_backup')||'');const [msg,setMsg]=useState('');const create=()=>{const data={version:1,createdAt:new Date().toISOString(),data:{}};keys.forEach(k=>{const v=localStorage.getItem(k);if(v!==null)data.data[k]=v});data.data.bazaar_products=JSON.stringify(loadProducts());const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`bazaar-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(url);const now=new Date().toLocaleString('ar-DZ');localStorage.setItem('bazaar_last_backup',now);setLast(now);setMsg('تم إنشاء النسخة الاحتياطية بنجاح');};const restore=async()=>{try{if('showOpenFilePicker' in window){const [handle]=await window.showOpenFilePicker({startIn:'downloads',multiple:false,types:[{description:'نسخة احتياطية للبرنامج',accept:{'application/json':['.json']}}]});const file=await handle.getFile();applyRestore(file);return;}document.getElementById('backupFileInput')?.click();}catch(error){if(error?.name!=='AbortError')setMsg('تعذر فتح نافذة اختيار النسخة. يمكنك اختيار الملف يدويًا.');}};const applyRestore=file=>{if(!file)return;const reader=new FileReader();reader.onload=()=>{try{const backup=JSON.parse(reader.result);if(!backup?.data)throw new Error();Object.entries(backup.data).forEach(([k,v])=>localStorage.setItem(k,typeof v==='string'?v:JSON.stringify(v)));setMsg('تم استعادة النسخة الاحتياطية. حدّث الصفحة لرؤية البيانات.');}catch{setMsg('الملف غير صالح أو ليس نسخة من البرنامج.')}};reader.readAsText(file);};return <div className="backupView"><div className="backupHeader"><h1><Database/> النسخ الاحتياطي</h1><p>حفظ واستعادة بيانات البرنامج بسهولة.</p></div><div className="backupGrid"><section className="backupCard primary"><div className="backupIcon"><ShieldCheck/></div><div><h2>إنشاء نسخة احتياطية</h2><p>حفظ بيانات السلع والمخزون والعملاء والمبيعات في ملف آمن على جهازك.</p></div><button onClick={create}><Download size={18}/> إنشاء نسخة فورية</button></section><section className="backupCard"><div className="backupCardHead"><Clock/><div><h2>آخر نسخة احتياطية</h2><strong>{last||'لا توجد نسخة بعد'}</strong></div></div><p>ننصح بإنشاء نسخة احتياطية بشكل دوري.</p></section><section className="backupCard"><div className="backupCardHead"><Upload/><div><h2>استعادة نسخة</h2><strong>رفع ملف النسخة الاحتياطية</strong></div></div><p>يفتح اختيار النسخة مباشرة من مجلد التنزيلات عند دعم المتصفح لذلك.</p><button className="uploadBtn" onClick={restore}><Upload size={17}/> اختيار نسخة احتياطية</button><input id="backupFileInput" type="file" accept="application/json,.json" onChange={e=>applyRestore(e.target.files?.[0])} style={{display:'none'}}/></section><section className="backupCard"><div className="backupCardHead"><RefreshCw/><div><h2>النسخ التلقائي</h2><strong>متاح لاحقًا</strong></div></div><p>سنضيف الجدولة اليومية والأسبوعية والشهرية في مرحلة الإعدادات.</p></section></div>{msg&&<div className="backupMessage">{msg}</div>}</div>}
